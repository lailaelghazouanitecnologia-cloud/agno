// Factorial function
fn factorial(n) {
    if (n <= 1) {
        return 1;
    }
    return n * factorial(n - 1);
}

print("Factorials:");
for (let i = 0; i <= 10; i = i + 1) {
    print(str(i) + "! = " + str(factorial(i)));
}